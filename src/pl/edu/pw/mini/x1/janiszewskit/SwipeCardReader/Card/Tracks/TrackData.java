package pl.edu.pw.mini.x1.janiszewskit.SwipeCardReader.Card.Tracks;

/**
 * Possible fields that can be stored on card
 */
public enum TrackData {
    NAME,
    EXPIRATION_DATE,
    FIRST_NAME,
    LAST_NAME,
    SERVICE_CODE,
    DISCRETIONAL_DATA,
    PRIMARY_ACCOUNT_NUMBER
}
